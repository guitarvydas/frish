bracketmatch {
  main = char+ end
  char =
    | "⎩" digit+ "⎭" -- line
    | "{" -- leftbrace
    | "(" -- leftparen
    | "}" -- rightbrace
    | ")" -- rightparen
    | ~"(" ~")" ~"{" ~"}" any -- other
}
