  function docreate () {
      defsynonym parameterAddress ≡ λlen (State.RAM) + 4
      %push (parameterAddress)
  }
  function create (name) {
      defsynonym normal ≡ 0
      λcode (name, normal, ↪︎docreate)
  }

  subr drop {
      ⌈ ( a -- ) ⌉
      %pop
  }

initialize {
  λok ()
}
